1.注册登陆界面功能点
用户填入用户名和密码进行登陆，如果账号不存在，则自动新建账号再登陆，同时需要填入网关服的IP和端口，在登陆成功后，再连接网关服，获取连接服IP地址，再连接连接服进行验证等操作，可以选择是否记住填写的信息，同时最好有一个状态栏可以显示当前状态信息。

2.协议测试界面功能点
分两个界面，一个显示请求(request)响应(response)和通知(notify)，另一个显示服务器推送(push)

请求和响应包括的功能点：

有一个单选框可以选择测试的接口是通知接口，因为大部分是请求响应式的
接口名，测试的接口名称是什么
请求参数，测试时需要发送给服务器的数据是什么
响应，服务器响应的数据是什么，如果选择了单选框，这个地方变灰色，这个时候服务器通过观察LOG进行测试。

推送包括的功能点：

接口名，监听一个接口，如果服务器推送过来数据，则显示出来，这个应该是可以添加多个监听项目的